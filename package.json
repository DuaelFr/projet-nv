{
  "name": "projet-nv",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:sass": "sass src/_sass:dist/css",
    "build:eleventy": "npx eleventy",
    "build": "npm-run-all build:*",
    "watch:sass": "sass --watch src/_sass:dist/css",
    "watch:eleventy": "npx eleventy --serve",
    "watch": "npm-run-all build:sass --parallel watch:*",
    "start": "npm run watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-img": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0"
  }
}
